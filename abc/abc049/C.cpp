#include <bits/stdc++.h>

using namespace std;

int main(){
    string s;
    cin>>s;
    string words[] = ["maerd", "remaerd", "esare", "resare"];

    int index = s.size() - 1;
    string ans = "YES"
    while(i >= 0){
        int j = i;
        while(i-j <= 7){
            if([j]
        }
    }

    cout<<ans<<endl;
}