#include <bits/stdc++.h>

using namespace std;

int main(){

	long long int h, w, n;
	cin>>h>>w>>n;
	
	typedef pair<long long int , long long int> pair;
	vector<pair> pos(n); 
	for(int i = 0; i < n; i++) cin>>pos[i].first>>pos[i].second; 

	for(int i = 0; i<n; i++){
		
	}
}
